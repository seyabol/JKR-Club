<% if (errors && errors.length > 0) { %>
  <ul class="mb-6 bg-red-600/10 border border-red-600 text-red-300 px-4 py-3 rounded shadow-sm space-y-1 text-sm">
    <% errors.forEach(function(error) { %>
      <li>🚨 <%= error.msg || error %></li>
    <% }); %>
  </ul>
<% } %>
