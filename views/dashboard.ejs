<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard | JKR Club</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gradient-to-br from-gray-950 via-slate-900 to-gray-800 text-white min-h-screen font-sans tracking-wide relative">

    <!-- Floating Return Button -->
    <a href="/" class="fixed top-6 left-6 z-50 bg-gray-800 hover:bg-gray-700 text-sm px-4 py-2 rounded-full shadow-lg border border-gray-700 transition">
      ← Return to Club Home
    </a>

    <div class="max-w-3xl mx-auto py-16 px-6">
      <% if (success && success.length > 0) { %>
        <div class="mb-6 bg-green-600/10 text-green-300 border border-green-600 px-4 py-3 rounded shadow text-sm">
          ✅ <%= success[0] %>
        </div>
      <% } %>

      <!-- Section Header -->
      <h1 class="text-3xl font-extrabold text-center mb-10 bg-clip-text text-transparent bg-gradient-to-r from-rose-500 to-purple-400 drop-shadow-md">
        🎉 Welcome Back, <%= user.full_name.split(" ")[0] %>!
      </h1>

      <!-- Profile Card -->
      <div class="flex flex-col items-center bg-gradient-to-tr from-slate-800 to-gray-900 rounded-2xl p-6 shadow-lg mb-10">
        <div class="bg-rose-600 w-20 h-20 rounded-full flex items-center justify-center text-4xl font-bold ring-4 ring-rose-400/30">
          <%= user.full_name[0].toUpperCase() %>
        </div>
        <h2 class="text-2xl font-semibold mt-4"><%= user.full_name %></h2>
        <p class="text-sm text-gray-400 mt-1">@<%= user.username %></p>
        <p class="text-sm text-gray-400">📧 <%= user.email %></p>

        <% if (user.is_admin) { %>
          <div class="mt-2 inline-block px-3 py-1 text-xs bg-red-600 rounded-full">🛡️ Admin</div>
        <% } else if (user.is_member) { %>
          <div class="mt-2 inline-block px-3 py-1 text-xs bg-purple-600 rounded-full">🎫 Member</div>
        <% } else { %>
          <div class="mt-2 inline-block px-3 py-1 text-xs bg-gray-600 rounded-full">🚫 Guest</div>
        <% } %>
      </div>

      <!-- Actions Header -->
      <h2 class="text-xl font-semibold text-rose-300 mb-4 text-center">🛠️ Your Actions</h2>

      <!-- Dashboard Buttons Grid -->
      <div class="grid sm:grid-cols-2 gap-4 mb-8">
        <a href="/create-message" class="w-full text-center bg-rose-600 hover:bg-rose-700 text-white py-2 rounded shadow transition">➕ Create Message</a>
        <a href="/logout" class="w-full text-center bg-red-600 hover:bg-red-700 text-white py-2 rounded shadow transition">🚪 Logout</a>

        <% if (!user.is_member && !user.is_admin) { %>
        <a href="/join-the-club" class="w-full text-center bg-purple-700 hover:bg-purple-800 py-2 rounded shadow transition">🌟 Become a Member</a>
        <% } %>

        <% if (!user.is_admin) { %>
        <a href="/become-admin" class="w-full text-center bg-red-700 hover:bg-red-800 py-2 rounded shadow transition">🔑 Become Admin</a>
        <% } %>
      </div>

      <!-- Footer Credits -->
      <div class="text-center text-sm text-gray-500 mt-12">
        Made by JKR 🃏
      </div>
    </div>
  </body>
</html>
