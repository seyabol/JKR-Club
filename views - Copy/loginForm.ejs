<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <title>Login | JKR Club</title>
      <link rel="stylesheet" href="/styles/style2.css" />
      <link
         href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500&display=swap"
         rel="stylesheet"
      />
   </head>
   <body class="neon-theme">
      <div class="form-container">
         <h1>JKR Club</h1>
         <h3>Login to the Mystery</h3>
         <h3 class="error"><%- include("partials/errors.ejs") %></h3>
         <% if (success && success.length > 0) { %>
            <div class="flash-success"><%= success[0] %></div>
         <% } %>

         <form action="/login" method="POST">
            <div class="form-group">
               <input
                  type="text"
                  name="identifier"
                  placeholder="Email or Username"
                  value="<%= oldInput ? oldInput.identifier : '' %>"
                  required
               />
            </div>

            <div class="form-group">
               <input
                  type="password"
                  name="password"
                  placeholder="Password"
                  required
               />
            </div>

            <div class="form-group">
               <button type="submit" class="glow-button">Login</button>
            </div>
         </form>

         <div style="text-align: center; margin-top: 20px">
            <a
               href="/"
               style="color: rgb(82, 98, 168); text-decoration: underline"
               >← Back to Home</a
            >
         </div>
      </div>
   </body>
</html>
