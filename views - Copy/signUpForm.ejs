<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <title><%= title %></title>
      <link rel="stylesheet" href="/styles/style2.css" />
      <link
         href="https://fonts.googleapis.com/css2?family=Orbitron&display=swap"
         rel="stylesheet"
      />
   </head>
   <body class="neon-theme">
      <div class="form-container">
         <h1>JKR Club Sign Up</h1>
         <h3 class="error"><%- include("partials/errors.ejs") %></h3>

         <form action="/signup" method="POST" class="neon-form">
            <div class="form-group">
               <input
                  type="text"
                  name="username"
                  placeholder="Username"
                  value="<%= oldInput ? oldInput.username : '' %>"
                  required
               />
            </div>

            <div class="form-group">
               <input
                  type="text"
                  name="fullName"
                  placeholder="Full Name"
                  value="<%= oldInput ? oldInput.fullName : '' %>"
                  required
               />
            </div>

            <div class="form-group">
               <input
                  type="email"
                  name="email"
                  placeholder="Email Address"
                  value="<%= oldInput ? oldInput.email : '' %>"
                  required
               />
            </div>

            <div class="form-group">
               <input
                  type="password"
                  name="password"
                  placeholder="password"
                  required
               />
            </div>

            <div class="form-group">
               <input
                  type="password"
                  name="confirmPassword"
                  placeholder="confirmPassword"
                  required
               />
            </div>

            <button type="submit" class="glow-button">Join the Club</button>
         </form>
      </div>
   </body>
</html>
